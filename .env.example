# URL base da API do backend
NUXT_PUBLIC_API_BASE=http://localhost:8080

# Chave publicável do Stripe (pk_test_... em sandbox, pk_live_... em produção).
# É pública por natureza — a chave secreta fica só no backend (STRIPE_SECRET_KEY).
NUXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
